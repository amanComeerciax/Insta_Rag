import { SavedPost } from '@/types';

// Static mock data removed per user preference - system now relies exclusively on real user data
export const SAMPLE_SAVED_POSTS: Omit<SavedPost, 'user_id'>[] = [];
